# 2026-09-13 — P8: docs consolidation (unification complete)

Docs only (no version bump). Branch `feature/unify-firmware-dsp`.

## What
- Root `README.md`: added a **Building the firmware** section (`./build.sh`, the
  6 variants, `-DMUX`/`-DDSP`), a `reboot-dfu` flashing tip, `version` +
  `reboot-dfu` in the command table, and a **DSP build (RP2350)** section
  (`read_raw`/`read_fft`, binary protocol, `pic0rick.dsp`, link to
  `docs/dsp_output_formats.md`).
- Marked the unification plan complete (P0–P8) and refreshed the branch memory.

## Status
The onboard-DSP unification (P0–P8) is done on `feature/unify-firmware-dsp`,
pushed, and hardware-verified at each phase. Firmware v0.1.9, python v0.1.3.
**Not merged to main** — a merge cuts release fw-v0.1.9 via CI, left to the
maintainer.

## Follow-ups (docs/claude/TODO.md)
- Rewrite a DSP test guide + `python/` example notebook against
  `read_raw`/`read_fft` + `pic0rick.dsp`.
- Optional: pulser scope validation; a standalone DSP capture CLI.
